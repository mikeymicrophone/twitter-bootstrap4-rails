require 'spec_helper'

describe Twitter::Bootstrap4::Rails do
  it 'has a version number' do
    expect(Twitter::Bootstrap4::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
